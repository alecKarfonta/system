# PlateForge — unified app on hostPort 8116 (k3s homelab overlay)
upstream host196_plateforge_api {
    server 127.0.0.1:8116;
    keepalive 8;
}

# Legacy alias; same backend serves UI and API
upstream host196_plateforge_fe {
    server 127.0.0.1:8116;
    keepalive 8;
}
