# Extended Proxy Timeouts (for APIs and file uploads)
# Include this in location blocks with: include /etc/nginx/snippets/proxy-timeout.conf;

proxy_read_timeout 300;
proxy_connect_timeout 300;
proxy_send_timeout 300;
