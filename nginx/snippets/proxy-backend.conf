# HTTP reverse-proxy defaults for upstream keepalive pools.
# Do NOT use on WebSocket locations (they need Connection: upgrade).
include /etc/nginx/snippets/proxy-params.conf;
proxy_http_version 1.1;
proxy_set_header Connection "";
