# Cross-Origin Isolation Headers (for WebGPU/SharedArrayBuffer)
# Include this in location blocks with: include /etc/nginx/snippets/cross-origin-isolation.conf;

add_header Cross-Origin-Embedder-Policy "require-corp" always;
add_header Cross-Origin-Opener-Policy "same-origin" always;
