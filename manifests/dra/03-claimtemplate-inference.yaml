apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: one-inference-gpu
spec:
  spec:
    devices:
      requests:
        - name: gpu
          exactly:
            deviceClassName: inference-gpu
            count: 1
